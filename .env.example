# Copy to .env and fill in ONLY when you intend to go live.
# .env is gitignored. Never commit real keys.

# --- Polymarket (CLOB) ---
# Wallet private key used to sign orders. Use a dedicated, low-balance wallet.
POLYMARKET_PRIVATE_KEY=
# Polygon address of that wallet.
POLYMARKET_FUNDER_ADDRESS=
# API creds derived from the wallet (py-clob-client can generate these).
POLYMARKET_API_KEY=
POLYMARKET_API_SECRET=
POLYMARKET_API_PASSPHRASE=

# CLOB host (default mainnet)
POLYMARKET_CLOB_HOST=https://clob.polymarket.com
POLYMARKET_CHAIN_ID=137

# --- Kalshi (trade-api/v2) ---
# Set venue: kalshi in config/config.yaml to trade here instead of Polymarket.
# API key id from the Kalshi dashboard.
KALSHI_API_KEY_ID=
# RSA private key for request signing: provide EITHER a file path...
KALSHI_PRIVATE_KEY_PATH=
# ...OR the PEM contents inline (path takes precedence if both are set).
KALSHI_PRIVATE_KEY_PEM=
# API host (default = live exchange).
KALSHI_HOST=https://api.elections.kalshi.com/trade-api/v2
